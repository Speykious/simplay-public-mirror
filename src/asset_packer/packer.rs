#![allow(dead_code)]

use std::io;

/// Convert the user-readable/user-editable resource pack to a format the game can understand, and add it to the cache.
pub fn install_pack() -> Result<(), io::Error> {
    return Ok(()); // todo
}
